# Note: In production, use Sealed Secrets or External Secrets Operator
# This is for demonstration purposes only

apiVersion: v1
kind: Secret
metadata:
  name: database-secrets
  namespace: testrails-staging
type: Opaque
stringData:
  username: testrails
  password: "CHANGE_ME_IN_PRODUCTION"
  url: "postgresql://testrails:CHANGE_ME_IN_PRODUCTION@postgres:5432/testrails"
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: testrails-staging
type: Opaque
stringData:
  secret-key: "CHANGE_ME_IN_PRODUCTION_USE_LONG_RANDOM_STRING"
  allowed-hosts: "staging.testrails.example.com"
